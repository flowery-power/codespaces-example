# .github/workflows/09-functions.yaml
name: 09 - Using Functions

on:
  workflow_dispatch:

jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      # PR info (will print empty/undefined unless this run is from a PR event)
      - name: Print PR title
        run: echo "PR title: ${{ github.event.pull_request.title }}"

      - name: Print PR labels
        run: |
          cat << 'EOF'
          ${{ toJSON(github.event.pull_request.labels) }}
          EOF

      - name: Bug step
        if: ${{ failure() && !cancelled() && contains(github.event.pull_request.title, 'fix') }}
        run: echo "I am a bug fix"

      # Give time to cancel from the UI
      - name: Sleep for 20 seconds
        run: sleep 20

      - name: Failing step
        run: exit 1

      - name: I will be skipped
        if: ${{ success() }}
        run: echo "I will print if previous steps succeed."

      - name: I will execute
        if: ${{ failure() }}
        run: echo "I will print if any previous step fails."

      - name: I will execute (not cancelled)
        if: ${{ !cancelled() }}
        run: echo "I will always print, except when the workflow is cancelled."

      - name: I will execute when cancelled
        if: ${{ cancelled() }}
        run: echo "I will execute when the workflow is cancelled."
